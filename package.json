{
  "name": "university-reports-creator",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate": "buf generate --template buf.gen.yaml",
    "lint:proto": "buf lint",
    "format:proto": "buf format -w",
    "generate:go": "buf generate --template buf.gen.yaml"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.67.0",
    "@nx-go/nx-go": "^3.3.1",
    "@nx/js": "^20.0.0",
    "nx": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
