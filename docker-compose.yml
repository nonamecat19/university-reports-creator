services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-auth}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 3s
      retries: 5

  surrealdb:
    image: surrealdb/surrealdb:latest
    pull_policy: always
    user: root
    restart: unless-stopped
    command: start --user root --pass root surrealkv:/data
    volumes:
      - surrealdb_data:/data
    ports:
      - "8000:8000"

  surrealdb-health:
    image: alpine:latest
    restart: "no"
    command: ["sleep", "infinity"]
    depends_on:
      surrealdb:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://surrealdb:8000/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 3s
      retries: 5

  service-auth:
    build:
      context: .
      dockerfile: service-auth/Dockerfile
    container_name: service-auth
    restart: unless-stopped
    environment:
      GRPC_PORT: ":50051"
      JWT_SECRET: ${JWT_SECRET:-change-me-to-a-strong-secret}
      ACCESS_TOKEN_DURATION: ${ACCESS_TOKEN_DURATION:-15m}
      REFRESH_TOKEN_DURATION: ${REFRESH_TOKEN_DURATION:-168h}
      DATABASE_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-auth}?sslmode=disable
    ports:
      - "50051:50051"
    depends_on:
      postgres:
        condition: service_healthy

  service-document:
    build:
      context: .
      dockerfile: service-document/Dockerfile
    container_name: service-document
    restart: unless-stopped
    environment:
      GRPC_PORT: ":50052"
      SURREALDB_URL: ws://surrealdb:8000
      SURREALDB_USERNAME: root
      SURREALDB_PASSWORD: root
      SURREALDB_NAMESPACE: diploma
      SURREALDB_DATABASE: main
    ports:
      - "50052:50052"
    depends_on:
      surrealdb-health:
        condition: service_healthy

  service-files:
    build:
      context: .
      dockerfile: service-files/Dockerfile
    container_name: service-files
    restart: unless-stopped
    environment:
      GRPC_PORT: ":50053"
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      MINIO_BUCKET: files
      MINIO_USE_SSL: "false"
    ports:
      - "50053:50053"
    depends_on:
      minio:
        condition: service_started

  service-gateway:
    build:
      context: .
      dockerfile: service-gateway/Dockerfile
    container_name: service-gateway
    restart: unless-stopped
    environment:
      HTTP_PORT: ":8080"
      AUTH_ADDR: service-auth:50051
      DOC_ADDR: service-document:50052
      FILES_ADDR: service-files:50053
      JWT_SECRET: ${JWT_SECRET:-change-me-to-a-strong-secret}
    ports:
      - "8080:8080"
    depends_on:
      - service-auth
      - service-document
      - service-files

volumes:
  pgdata:
  surrealdb_data:
  minio_data:
